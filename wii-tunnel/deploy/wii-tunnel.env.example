# Copy to /etc/butaningen/wii-tunnel.env and chmod 600. NEVER commit real values.
# TUNNEL_SECRET must match the same env var on the Render relay.

RELAY_WS_URL=wss://line-bot-mcp-server-4kvf.onrender.com/_tunnel
TUNNEL_SECRET=
LOCAL_TARGET=http://127.0.0.1:10000
